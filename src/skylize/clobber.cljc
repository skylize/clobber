(ns skylize.clobber
  (:require [skylize.poj :as p]))

(defn foo
  "I don't do a whole lot."
  [& _args]
  (prn "CLI interface not currently implemented. See README.md for usage as a Clojure library."))
